DEMO VIDEO — landing page (#video section on home.php)

Drop two files here to make the "See a Real Order in 60 Seconds" section appear:

  demo.mp4          (required)  — your screen recording, H.264/MP4, 16:9, ~30-60s.
                                  Keep it small: 720p or 1080p, aim under ~15 MB so it
                                  loads fast. The section stays HIDDEN until this exists.

  demo-poster.jpg   (optional)  — a still thumbnail shown before play (16:9, e.g.
                                  1280x720). If missing, the first frame is used.

That's it — no code changes needed. The play button, controls and lazy-loading
(preload="none") are already wired. The URL auto-cache-busts on file change.
